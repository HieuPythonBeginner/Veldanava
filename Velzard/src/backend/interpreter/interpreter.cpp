#include "interpreter.h"
#include <iostream>
namespace interpreter {
    int eval(ast::ProgramNode* program) {
        std::cout << "[Velzard Interpreter] Running program\n";
        return 0;
    }
}
