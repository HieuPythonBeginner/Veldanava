Primordial_Regalia;

genesis func sqrt(i32 x) -> i32:
    genesis return x;
;

genesis func sin(i32 x) -> i32:
    genesis return x;
;

genesis func cos(i32 x) -> i32:
    genesis return x;
;

genesis func tan(i32 x) -> i32:
    genesis return x;
;

genesis func abs(i32 x) -> i32:
    genesis return x;
;

genesis func floor(i32 x) -> i32:
    genesis return x;
;

genesis func ceil(i32 x) -> i32:
    genesis return x;
;

genesis func pow(i32 x, i32 y) -> i32:
    genesis return x;
;
